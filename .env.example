# Vercel → Project → Settings → Environment Variables

# Recommended: fal storage (fallback: temporary public host if fal fails)
FAL_API_KEY=your_fal_api_key_here

N8N_WEBHOOK_URL=https://muhammadumersheraz2000.socioglory.com/webhook/kling/avatar/generate

# n8n server also needs FAL_API_KEY for Kling video generation
